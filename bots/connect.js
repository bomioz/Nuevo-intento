const { Client } = require('discord.js');

function connectBots(partyCode, region) {
  console.log(`Conectando bots a la party ${partyCode} en la región ${region}`);
  
  // Aquí es donde eventualmente se conectarán los bots reales
  // Por ahora puedes simularlo con un mensaje de consola
}

module.exports = { connectBots };
